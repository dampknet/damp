import PageLoader from "@/components/PageLoader";

export default function Loading() {
  return (
    <PageLoader
      title="Loading dashboard"
      subtitle="Preparing site, asset, and activity summaries..."
    />
  );
}
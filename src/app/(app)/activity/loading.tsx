import PageLoader from "@/components/PageLoader";

export default function Loading() {
  return (
    <PageLoader
      title="Loading activity"
      subtitle="Fetching recent system activity..."
    />
  );
}